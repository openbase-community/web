services:
  redis:
    image: redis
    ports:
      - 6379:6379

  db:
    image: pgvector/pgvector:pg14
    volumes:
      - ../data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=appdata
    ports:
      - 5432:5432

  caddy:
    build:
      context: ./caddy
      dockerfile: Dockerfile
    ports:
      - 80:80
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - PROXY_TARGET=${PROXY_TARGET:-host.docker.internal:8000}
      - FRONTEND_PROXY_TARGET=${FRONTEND_PROXY_TARGET:-host.docker.internal:8080}
